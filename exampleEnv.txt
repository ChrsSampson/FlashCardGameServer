Should be named .env.[ development || production || test ].local and placed in the root of the project.

PORT=[Your Port]
MODE=[environment mode] 
PGHOST=[URL]
PGPORT=[port]
PGUSER=[username]
PGPASSWORD=[password]
PGDATABASE=[database name]